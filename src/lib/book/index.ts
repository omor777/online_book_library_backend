import {
  create,
  count,
  findAllItems,
  findSingleItem,
  updateOrCreate,
  updateProperties,
} from "./service";

export const services = {
  create,
  findAllItems,
  count,
  findSingleItem,
  updateOrCreate,
  updateProperties,
};
