import {
  create,
  count,
  findAllItems,
  findSingleItem,
  updateOrCreate,
} from "./service";

export const services = {
  create,
  findAllItems,
  count,
  findSingleItem,
  updateOrCreate,
};
