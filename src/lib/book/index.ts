import {
  create,
  count,
  findAllItems,
  findSingleItem,
  updateOrCreate,
  updateProperties,
  deleteItem,
} from "./service";

export const services = {
  create,
  findAllItems,
  count,
  findSingleItem,
  updateOrCreate,
  updateProperties,
  deleteItem,
};
