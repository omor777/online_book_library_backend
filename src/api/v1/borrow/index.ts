import {
  create,
  findAllItemsAdmin,
  acceptBorrowRequest,
  rejectBorrowRequest,
} from "./controller";

export const controllers = {
  create,
  findAllItemsAdmin,
  acceptBorrowRequest,
  rejectBorrowRequest,
};
